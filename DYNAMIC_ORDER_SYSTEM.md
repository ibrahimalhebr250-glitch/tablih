# نظام إنشاء الطلبات الديناميكي - التوثيق الكامل

## نظرة عامة

تم بناء نظام إنشاء طلبات ديناميكي متكامل يتصل مباشرة بقاعدة البيانات ويتحدث لحظياً عند أي تغيير يحدث من لوحة التحكم.

---

## 🔄 التحديثات اللحظية (Realtime)

النظام يستخدم Supabase Realtime للاستماع للتغييرات على 7 جداول:

### الجداول المراقبة:
1. **pallet_types_master** - أنواع الطبليات
2. **pallet_sizes_master** - مقاسات الطبليات
3. **quality_grades_master** - درجات الجودة
4. **cities** - المدن
5. **flexibility_options_settings** - خيارات المرونة
6. **quantity_settings** - إعدادات الكمية
7. **order_types_settings** - أنواع الطلبات

### كيفية العمل:
- أي تغيير في لوحة التحكم (إضافة، تعديل، حذف، تفعيل/تعطيل)
- يتم تطبيقه **فوراً** على صفحة إنشاء الطلب
- بدون الحاجة لإعادة تحميل الصفحة

---

## 📊 البيانات المتاحة حالياً

### 1. أنواع الطبليات (3)
```
🪵 خشبية (wood)
🔷 بلاستيكية (plastic)
♻️ إعادة تدوير (recycled)
```

### 2. مقاسات الطبليات (9)
**خشبية:**
- 120×100 (حمولة 1500 كجم)
- 110×110 (حمولة 1200 كجم)
- 120×80 (حمولة 1000 كجم)
- 80×60 (حمولة 500 كجم)

**بلاستيكية:**
- 120×100 (حمولة 1200 كجم)
- 110×110 (حمولة 1000 كجم)
- 120×80 (حمولة 800 كجم)

**إعادة تدوير:**
- 120×100 (حمولة 1000 كجم)
- 110×110 (حمولة 900 كجم)

### 3. درجات الجودة (4)
- **Grade A** - ممتازة (حالة ممتازة - جديدة أو شبه جديدة)
- **Grade B** - جيدة (حالة جيدة - مستعملة قليلاً)
- **Grade C** - مقبولة (حالة مقبولة - مستعملة)
- **Grade Scrap** - خردة (للتخلص أو إعادة التدوير)

### 4. المدن (16)
الرياض، جدة، مكة المكرمة، المدينة المنورة، الدمام، الخبر، الأحساء، تبوك، بريدة، خميس مشيط، الطائف، ينبع، الجبيل، حائل، نجران، أبها

### 5. خيارات المرونة (3)
- قبول جودة قريبة
- قبول مدن قريبة
- قبول توصيل جزئي

---

## 🏗️ البنية التقنية

### Hook الرئيسي: `useDynamicOrderBuilder`

**الموقع:** `src/hooks/useDynamicOrderBuilder.ts`

**الوظائف:**
```typescript
{
  orderTypes: DynamicOrderType[],
  palletTypes: DynamicPalletType[],
  palletSizes: DynamicPalletSize[],
  qualityGrades: DynamicQualityGrade[],
  cities: DynamicCity[],
  flexibilityOptions: FlexibilityOption[],
  quantitySettings: QuantitySettings,
  loading: boolean,
  error: string | null,
  getSizesForPalletType: (code: string) => DynamicPalletSize[],
  refresh: () => Promise<void>
}
```

### المكونات المرتبطة:

1. **TypeSelector** - اختيار نوع الطبلية
   - يعرض الأيقونات والأسماء من قاعدة البيانات
   - يرسل `code` عند الاختيار

2. **SizeSelector** - اختيار المقاس
   - يتغير ديناميكياً حسب نوع الطبلية
   - يعرض الحمولة القصوى لكل مقاس

3. **QualitySelector** - اختيار الجودة
   - يعرض الدرجات مع الأوصاف
   - ألوان مخصصة لكل درجة

4. **CitySelector** - اختيار المدينة
   - قائمة منسدلة مع بحث
   - تعرض المدن النشطة فقط

5. **QuantityInput** - إدخال الكمية
   - يستخدم إعدادات الكمية من قاعدة البيانات
   - أزرار سريعة حسب القيم الافتراضية

6. **FlexibilityToggle** - خيارات المرونة
   - تعرض الخيارات النشطة فقط
   - تبديل بسيط لكل خيار

---

## 🎯 آلية الفلترة الذكية

### فلترة المقاسات حسب النوع:
```typescript
const getSizesForPalletType = (palletTypeCode: string) => {
  return palletSizes.filter(size => size.pallet_type_code === palletTypeCode);
};
```

عندما يختار المستخدم نوع طبلية، تظهر **فقط** المقاسات المتاحة لهذا النوع.

---

## 🔐 الأمان والأداء

### سياسات RLS:
- جميع الجداول محمية بـ RLS
- القراءة متاحة للجميع (anon)
- التعديل متاح للأدمن فقط

### التحسينات:
- تحميل جميع البيانات دفعة واحدة (Promise.all)
- Realtime subscriptions لتحديثات لحظية
- Cleanup عند إلغاء Component
- Loading states لتحسين تجربة المستخدم

---

## 📝 التكامل مع OrderBuilder

### الاستخدام في OrderBuilder:
```tsx
const dynamicData = useDynamicOrderBuilder();

// عرض شاشة تحميل
if (dynamicData.loading) {
  return <LoadingScreen />;
}

// تمرير البيانات للمكونات
<TypeSelector
  palletTypes={dynamicData.palletTypes}
  selected={form.palletType}
  onSelect={setPalletType}
/>
```

---

## ✅ اختبار النظام

### خطوات الاختبار:
1. افتح صفحة إنشاء طلب جديد
2. تحقق من ظهور جميع الخيارات
3. افتح لوحة التحكم > إدارة الطلبات
4. قم بتعديل أي إعداد (مثل: تعطيل مقاس معين)
5. ارجع لصفحة إنشاء الطلب
6. **يجب أن يختفي المقاس المعطل فوراً**

### نقاط التحقق:
- ✅ جميع أنواع الطبليات تظهر
- ✅ المقاسات تتغير حسب النوع
- ✅ درجات الجودة كاملة مع الأوصاف
- ✅ المدن متاحة للاختيار
- ✅ خيارات المرونة تعمل
- ✅ إعدادات الكمية صحيحة
- ✅ التحديثات اللحظية تعمل

---

## 🔧 الصيانة والتطوير

### إضافة نوع طبلية جديد:
1. ادخل لوحة التحكم > إدارة المخزون > أنواع الطبليات
2. أضف النوع الجديد مع الأيقونة
3. سيظهر تلقائياً في صفحة إنشاء الطلب

### إضافة مقاس جديد:
1. ادخل لوحة التحكم > إدارة المخزون > مقاسات الطبليات
2. أضف المقاس وحدد نوع الطبلية
3. سيظهر تلقائياً عند اختيار هذا النوع

### تعطيل خيار:
1. غيّر حالة الخيار إلى "غير نشط"
2. سيختفي فوراً من صفحة إنشاء الطلب

---

## 🎨 التصميم والألوان

### ألوان درجات الجودة:
- **Grade A**: أخضر (#27AE60)
- **Grade B**: أزرق (#2196F3)
- **Grade C**: برتقالي (#F59E0B)
- **Grade Scrap**: رمادي

### الحالات التفاعلية:
- Hover effects
- Active states
- Selected indicators
- Loading states
- Error states

---

## 📱 الاستجابة (Responsive)

النظام يعمل بكفاءة على:
- 📱 الجوالات (شاشات صغيرة)
- 💻 الأجهزة اللوحية (شاشات متوسطة)
- 🖥️ أجهزة الكمبيوتر (شاشات كبيرة)

---

## 🚀 الخلاصة

تم بناء نظام ديناميكي متكامل:
- ✅ مربوط بالكامل مع قاعدة البيانات
- ✅ تحديثات لحظية (Realtime)
- ✅ فلترة ذكية للمقاسات
- ✅ واجهة مستخدم احترافية
- ✅ أمان وحماية كاملة
- ✅ أداء محسّن
- ✅ جاهز للإنتاج

أي تعديل في لوحة التحكم يظهر **فوراً** على صفحة إنشاء الطلب بدون أي تأخير!
