# نظام الجلسات المستمرة

## نظرة عامة
تم تطبيق نظام جلسات متقدم يسمح للمستخدمين بالبقاء مسجلين في المنصة لفترة طويلة دون الحاجة لإعادة تسجيل الدخول.

## المكونات الرئيسية

### 1. قاعدة البيانات

#### جدول `user_sessions`
يحفظ جميع الجلسات النشطة:
- `session_token`: رمز فريد مشفر للجلسة
- `phone`: رقم هاتف المستخدم
- `user_type`: نوع المستخدم (buyer/supplier/admin)
- `expires_at`: تاريخ انتهاء الجلسة
- `last_activity_at`: آخر نشاط للمستخدم
- `is_active`: هل الجلسة نشطة

#### إعدادات المنصة
تم إضافة `session_duration_hours` في جدول `platform_settings`:
- القيمة الافتراضية: 24 ساعة
- قابلة للتعديل من لوحة الإدارة

### 2. دوال قاعدة البيانات

#### `create_user_session()`
- تنشأ جلسة جديدة عند تسجيل الدخول
- تُلغي الجلسات القديمة للمستخدم (جلسة واحدة فقط لكل مستخدم)
- تحدد مدة الجلسة حسب إعدادات المنصة

#### `validate_user_session()`
- تتحقق من صلاحية الجلسة
- تُحدّث `last_activity_at` تلقائياً
- ترجع معلومات المستخدم إذا كانت الجلسة صالحة

#### `invalidate_user_session()`
- تُلغي جلسة واحدة (تسجيل خروج)

#### `invalidate_all_user_sessions()`
- تُلغي جميع جلسات المستخدم (تسجيل خروج من كل الأجهزة)

#### `cleanup_expired_sessions()`
- تحذف الجلسات المنتهية تلقائياً
- يمكن جدولتها للتشغيل دورياً

### 3. الواجهة الأمامية

#### `sessionManager` (`src/lib/sessionManager.ts`)
مدير الجلسات في الواجهة:
- `createSession()`: إنشاء جلسة جديدة
- `validateSession()`: التحقق من الجلسة
- `invalidateSession()`: إنهاء الجلسة
- `hasSession()`: فحص وجود جلسة
- `getSessionToken()`: الحصول على رمز الجلسة

#### تكامل مع `useSession`
- عند تسجيل الدخول/التسجيل: ينشئ جلسة تلقائياً
- عند تحميل التطبيق: يتحقق من الجلسة النشطة
- إذا وجد جلسة صالحة: يسجل دخول المستخدم تلقائياً
- عند تسجيل الخروج: يُلغي الجلسة

### 4. لوحة الإدارة

#### تبويب "إدارة الجلسات"
موجود في: `الإعدادات → إدارة الجلسات`

**الإحصائيات:**
- مدة الجلسة الحالية
- عدد الجلسات النشطة
- إجمالي المستخدمين
- نسبة المستخدمين المتصلين

**الخيارات السريعة:**
- 6 ساعات
- 12 ساعة
- 24 ساعة (افتراضي)
- 48 ساعة (يومين)
- 72 ساعة (3 أيام)
- 168 ساعة (أسبوع)

**مدة مخصصة:**
- من 1 إلى 720 ساعة (شهر تقريباً)

## كيف يعمل النظام؟

### عند تسجيل الدخول:
```
1. المستخدم يدخل رقم الجوال والرقم السري
2. النظام يتحقق من البيانات
3. يتم إنشاء session_token فريد
4. يُحفظ Token في localStorage
5. يتم حفظ بيانات الجلسة في قاعدة البيانات
6. المستخدم يُوجّه للصفحة الرئيسية
```

### عند فتح المنصة:
```
1. التطبيق يقرأ session_token من localStorage
2. يرسل Token للتحقق منه في قاعدة البيانات
3. إذا كان صالحاً: تسجيل دخول تلقائي
4. إذا انتهى: طلب تسجيل دخول جديد
```

### أثناء الاستخدام:
```
- كل طلب يُحدّث last_activity_at تلقائياً
- المستخدم يتنقل بحرية دون قيود
- الجلسة تظل نشطة حتى انتهاء المدة
```

### عند تسجيل الخروج:
```
1. المستخدم يضغط "تسجيل الخروج"
2. يتم إلغاء الجلسة في قاعدة البيانات
3. يُحذف Token من localStorage
4. المستخدم يُوجّه للصفحة الرئيسية
```

## الأمان

- كل Session Token فريد ولا يمكن التنبؤ به (32 بايت عشوائي)
- الجلسات تنتهي تلقائياً بعد المدة المحددة
- يتم التحقق من صلاحية الجلسة عند كل استخدام
- المستخدم لا يمكنه الوصول لجلسات مستخدمين آخرين
- RLS Policies تحمي بيانات الجلسات

## رسائل Console للتتبع

عند تطوير التطبيق، يمكنك متابعة النظام من خلال رسائل Console:

```
🔐 Creating new session for: 0512345678
✅ Session created successfully. Expires at: 2026-03-08T10:00:00Z
⏱️ Session duration: 24 hours

🔍 Validating session...
✅ Session valid for user: محمد أحمد
⏱️ Expires at: 2026-03-08T10:00:00Z
```

## ملاحظات مهمة

1. **التغييرات تؤثر على الجلسات الجديدة فقط**
   - إذا غيرت مدة الجلسة من 24 إلى 12 ساعة
   - الجلسات الحالية تبقى على 24 ساعة
   - الجلسات الجديدة ستكون 12 ساعة

2. **جلسة واحدة لكل مستخدم**
   - عند تسجيل الدخول من جهاز جديد
   - يتم إلغاء الجلسة القديمة تلقائياً
   - للسماح بجلسات متعددة: احذف السطر في `create_user_session`

3. **تنظيف تلقائي**
   - الجلسات المنتهية تُحفظ لمدة 7 أيام
   - بعدها تُحذف تلقائياً عند تشغيل `cleanup_expired_sessions()`
   - يمكن جدولة هذه الدالة للتشغيل يومياً

## اختبار النظام

### اختبار تسجيل الدخول:
1. سجل دخول بحساب
2. افتح Console في المتصفح
3. ابحث عن رسائل 🔐 و ✅
4. تحقق من localStorage: `pallet_session_token`

### اختبار استمرار الجلسة:
1. سجل دخول
2. أغلق التبويب
3. افتح المنصة مرة أخرى
4. يجب أن تكون مسجل دخول تلقائياً

### اختبار انتهاء الجلسة:
1. في لوحة الإدارة، قلل مدة الجلسة لـ 6 ساعات
2. سجل خروج ثم دخول
3. انتظر 6 ساعات
4. حاول استخدام المنصة
5. يجب أن يطلب منك تسجيل الدخول

## استعلامات مفيدة

### عرض الجلسات النشطة:
```sql
SELECT
  phone,
  user_name,
  user_type,
  expires_at,
  last_activity_at,
  created_at
FROM user_sessions
WHERE is_active = true
ORDER BY last_activity_at DESC;
```

### عدد الجلسات لكل مستخدم:
```sql
SELECT
  user_type,
  COUNT(*) as active_sessions
FROM user_sessions
WHERE is_active = true
GROUP BY user_type;
```

### حذف الجلسات المنتهية يدوياً:
```sql
SELECT cleanup_expired_sessions();
```

### تحديث مدة الجلسة:
```sql
UPDATE platform_settings
SET session_duration_hours = 48
WHERE id = (SELECT id FROM platform_settings LIMIT 1);
```

## الدعم والمساعدة

إذا واجهت أي مشكلة:
1. افتح Console في المتصفح
2. ابحث عن رسائل ❌ أو ⚠️
3. تحقق من localStorage: هل `pallet_session_token` موجود؟
4. تحقق من قاعدة البيانات: هل الجلسة مسجلة؟
5. راجع إعدادات `session_duration_hours` في المنصة
