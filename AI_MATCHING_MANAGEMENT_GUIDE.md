# دليل إدارة نظام المطابقة الذكية

## نظرة عامة

هذا الدليل الشامل لنظام إدارة المطابقة بالذكاء الاصطناعي، الذي يوفر أدوات متقدمة للتحكم والمراقبة والتحليل والتدخل اليدوي في عملية المطابقة التلقائية.

---

## 📋 محتويات الدليل

1. [مركز التحكم](#مركز-التحكم)
2. [إدارة القواعد](#إدارة-القواعد)
3. [القائمة السوداء](#القائمة-السوداء)
4. [مراقبة الأداء](#مراقبة-الأداء)
5. [تحليل الأنماط](#تحليل-الأنماط)
6. [أدوات التدخل اليدوي](#أدوات-التدخل-اليدوي)
7. [التكامل مع الأقسام](#التكامل-مع-الأقسام)

---

## 🎛️ مركز التحكم

### الوظائف الرئيسية

مركز التحكم هو قلب نظام إدارة المطابقة، يوفر:

#### 1. المراقبة الحية (Real-time Monitoring)

- **الطلبات المعلقة**: عدد الطلبات في انتظار المطابقة
- **المخزون المتاح**: عدد الدفعات النشطة الجاهزة للمطابقة
- **المطابقات النشطة**: عدد الصفقات المطابقة حالياً
- **معدل النجاح (24 ساعة)**: نسبة المطابقات المقبولة

#### 2. التحكم في النظام

**تفعيل/تعطيل المطابقة التلقائية:**
```typescript
// يتم التحكم عبر واجهة المستخدم
isAutoMatchEnabled: boolean
```

عند التعطيل:
- توقف المطابقة التلقائية عند إضافة مخزون جديد
- يتم الاعتماد على المطابقة اليدوية فقط
- تستمر عمليات التتبع والإحصائيات

#### 3. النشاط الأخير

عرض آخر 5 محاولات مطابقة مع:
- حالة القبول/الرفض
- درجة المطابقة
- التوقيت

#### 4. صحة النظام

مراقبة حالة:
- محرك الذكاء الاصطناعي
- قاعدة البيانات
- نظام الإشعارات
- مستوى الأداء

---

## ⚙️ إدارة القواعد

### أنواع القواعد

#### 1. Weight Adjustment (تعديل الأوزان)

تعديل أوزان عوامل المطابقة لسيناريوهات معينة.

**مثال:**
```json
{
  "rule_name": "زيادة أهمية الجودة للخشب",
  "rule_type": "weight_adjustment",
  "priority": 10,
  "conditions": {
    "pallet_type": "خشبية"
  },
  "actions": {
    "multiplier": 1.2
  }
}
```

**التأثير:**
- يزيد جميع درجات المطابقة للطبليات الخشبية بنسبة 20%
- يعطي أولوية أعلى لهذا النوع

#### 2. Score Threshold (عتبة الدرجة)

تحديد حدود دنيا/عليا لدرجات المطابقة.

**مثال:**
```json
{
  "rule_name": "رفع الحد الأدنى للمطابقة",
  "rule_type": "score_threshold",
  "priority": 5,
  "conditions": {
    "min_score": 0,
    "max_score": 100
  },
  "actions": {
    "min_score": 75
  }
}
```

**التأثير:**
- لا يتم قبول أي مطابقة بدرجة أقل من 75
- يضمن جودة عالية للمطابقات

#### 3. Priority Rule (قاعدة الأولوية)

منح أولوية خاصة لمطابقات معينة.

**الاستخدامات:**
- تفضيل مدن معينة
- تفضيل موردين موثوقين
- تفضيل طلبات عاجلة

#### 4. Exclusion Rule (قاعدة الاستبعاد)

منع مطابقات معينة تماماً.

**مثال:**
```json
{
  "rule_name": "منع مطابقة جودة A مع Scrap",
  "rule_type": "exclusion",
  "conditions": {
    "order_quality": "A",
    "batch_quality": "Scrap"
  }
}
```

### الأولويات

القواعد تُطبق حسب الأولوية (الأعلى أولاً):
- **10-20**: أولوية قصوى (قواعد حرجة)
- **5-9**: أولوية عالية
- **0-4**: أولوية عادية
- **سالب**: أولوية منخفضة (تطبق أخيراً)

### إدارة القواعد

**إضافة قاعدة جديدة:**
1. انقر "Add Rule"
2. املأ التفاصيل:
   - اسم القاعدة
   - نوع القاعدة
   - الأولوية
   - الشروط
   - الإجراءات
3. احفظ

**تعديل قاعدة:**
- انقر على أيقونة التعديل
- عدّل البيانات
- احفظ التغييرات

**تفعيل/تعطيل:**
- انقر على أيقونة الطاقة
- التعطيل لا يحذف القاعدة

**حذف قاعدة:**
- انقر على أيقونة الحذف
- تأكيد الحذف

---

## 🛡️ القائمة السوداء

### أنواع الحظر

#### 1. Buyer-Supplier Block (حظر مشتري-مورد)

منع مطابقة بين مشتري ومورد محددين.

**متى يُستخدم:**
- نزاعات سابقة
- مشاكل في التسليم
- طلب أحد الطرفين

**البيانات:**
```typescript
{
  buyer_phone: "966501234567",
  supplier_phone: "966509876543",
  reason: "نزاع على جودة التسليم السابق",
  blacklist_type: "buyer_supplier"
}
```

#### 2. Quality Grade Block (حظر درجات جودة)

منع مطابقة بين درجتي جودة معينتين.

**مثال:**
```typescript
{
  conditions: {
    order_quality: "A",
    batch_quality: "C"
  },
  reason: "فرق كبير في الجودة",
  blacklist_type: "quality_grade"
}
```

#### 3. City Pair Block (حظر زوج مدن)

منع مطابقة بين مدينتين.

**مثال:**
```typescript
{
  conditions: {
    order_city: "الرياض",
    batch_city: "جدة"
  },
  reason: "مسافة طويلة، تكاليف شحن عالية",
  blacklist_type: "city_pair"
}
```

### الصلاحية الزمنية

يمكن تحديد تاريخ انتهاء للحظر:

```typescript
{
  expires_at: "2026-12-31T23:59:59Z"
}
```

**فوائد:**
- حظر مؤقت للنزاعات القابلة للحل
- إعادة تقييم تلقائية
- مرونة في الإدارة

### كيفية التحقق من الحظر

يتم التحقق تلقائياً قبل أي مطابقة:

```sql
SELECT check_matching_blacklist(
  buyer_phone,
  supplier_phone,
  order_quality,
  batch_quality,
  order_city,
  batch_city
);
```

---

## 📊 مراقبة الأداء

### المقاييس الرئيسية

#### 1. إحصائيات المحاولات

- **إجمالي المحاولات**: عدد جميع محاولات المطابقة
- **المطابقات الناجحة**: المقبولة والمكتملة
- **المطابقات الفاشلة**: المرفوضة أو الملغاة
- **معدل النجاح**: نسبة النجاح المئوية

#### 2. متوسط الدرجات

عرض متوسط درجات المطابقة لتقييم جودة النظام.

**نطاقات الأداء:**
- **90-100**: أداء ممتاز
- **80-89**: أداء جيد جداً
- **70-79**: أداء جيد
- **60-69**: أداء مقبول
- **أقل من 60**: يحتاج تحسين

#### 3. الأداء اليومي

رسم بياني يوضح:
- معدل النجاح اليومي
- عدد المحاولات
- المطابقات الناجحة/الفاشلة

#### 4. التوزيع حسب الفئات

- **حسب الجودة**: توزيع المطابقات لكل درجة جودة
- **حسب المدن**: توزيع جغرافي للمطابقات
- **حسب نطاق الدرجة**: توزيع على نطاقات الدرجات

### فترات التحليل

اختيار فترة التحليل:
- آخر 7 أيام
- آخر 14 يوم
- آخر 30 يوم

### تحديث المقاييس

يتم التحديث:
- **تلقائياً**: كل 30 ثانية
- **يدوياً**: عبر زر Refresh
- **عند الحدث**: بعد كل مطابقة جديدة

---

## 🔍 تحليل الأنماط

### أنماط النجاح

يحلل النظام أنماط المطابقات الناجحة لتحديد:

#### 1. أزواج الجودة الناجحة

**مثال:**
```
Quality A to A matches have 95% success rate
Recommendation: Prioritize these quality combinations
```

**الفوائد:**
- تحديد أفضل تطابقات الجودة
- زيادة احتمالية النجاح
- تحسين رضا العملاء

#### 2. المسارات الجغرافية المثلى

تحديد أزواج المدن الأكثر نجاحاً:
- معدلات التسليم الناجح
- أوقات التسليم
- تكاليف الشحن

#### 3. نطاقات الأسعار المفضلة

تحليل:
- نطاقات الأسعار الأكثر قبولاً
- حساسية السعر حسب الجودة
- نقاط التسعير المثلى

#### 4. أحجام الطلبات المثالية

- الكميات الأكثر طلباً
- معدلات إتمام الطلبات الجزئية
- نقاط التوازن بين العرض والطلب

### مستوى الثقة

كل نمط مُحدد يأتي مع **مستوى ثقة** (Confidence Score):
- **90-100%**: نمط مؤكد (عينة كبيرة)
- **70-89%**: نمط قوي
- **50-69%**: نمط محتمل
- **أقل من 50%**: بيانات غير كافية

### التوصيات الآلية

بناءً على الأنماط، يقدم النظام توصيات مثل:
- "أعط أولوية لهذه المجموعات"
- "ارفع حد الدرجة لهذه الفئة"
- "قلل الوزن لهذا العامل"

### تطبيق الأنماط

يمكن تحويل الأنماط المكتشفة إلى قواعد:
1. مراجعة النمط والثقة
2. إنشاء قاعدة مطابقة مناسبة
3. اختبار القاعدة
4. تفعيلها

---

## 🔧 أدوات التدخل اليدوي

### Force Match (فرض المطابقة)

#### متى تُستخدم

- طلب خاص من عميل مهم
- صفقة استراتيجية
- حالة طارئة
- اختبار النظام

#### كيفية الاستخدام

1. **اختر الطلب**:
   - من قائمة الطلبات المعلقة
   - أدخل معرف الطلب مباشرة

2. **اختر دفعة المخزون**:
   - من قائمة المخزون النشط
   - أدخل معرف الدفعة

3. **اكتب السبب**:
   ```
   مثال: "طلب عاجل من عميل VIP - الموافقة من المدير التنفيذي"
   ```

4. **تأكيد**:
   - انقر "Force Match"
   - سيتم إنشاء الصفقة فوراً

#### التأثيرات

```typescript
// يتم تنفيذ:
- إنشاء صفقة مباشرة
- تحديث حالة الطلب → 'matched'
- حجز الكمية من المخزون
- تسجيل التدخل في سجل التدقيق
- إرسال إشعارات للطرفين
```

#### التدقيق

جميع المطابقات المفروضة يتم تسجيلها في:

```sql
matching_overrides (
  order_id,
  batch_id,
  override_type: 'force_match',
  reason,
  admin_phone,
  created_at
)
```

### Block Match (حظر المطابقة)

#### متى تُستخدم

- منع مطابقة معينة مؤقتاً
- اختبار سلبي
- حل مشكلة معينة

#### كيفية الاستخدام

1. اختر الطلب والدفعة
2. اكتب سبب الحظر
3. انقر "Block Match"

#### التأثير

```typescript
// يتم:
- تسجيل الحظر
- منع المطابقة التلقائية بينهما
- عدم ظهور في قائمة المطابقات المقترحة
- الحظر دائم حتى الحذف اليدوي
```

### الصلاحيات المطلوبة

```typescript
// يتطلب:
- user_type = 'admin'
- permissions.market.can_edit = true
// أو
- role = 'system_admin'
```

### التحذيرات

⚠️ **استخدم بحذر**:
- التدخل اليدوي يتجاوز الذكاء الاصطناعي
- قد يؤثر على إحصائيات الأداء
- جميع الإجراءات مسجلة للمراجعة

---

## 🔗 التكامل مع الأقسام

### 1. مع قسم الطلبات

**الربط:**
- عرض حالة المطابقة لكل طلب
- إحصائيات المطابقة لكل مشتري
- أفضل المطابقات المقترحة

**واجهة برمجية:**
```typescript
// في OrdersSection
<SmartMatchingPanel
  orderId={order.id}
  onSelectMatch={(batchId, score) => {
    // معالجة المطابقة المختارة
  }}
/>
```

### 2. مع قسم المخزون

**الربط:**
- تتبع المطابقات لكل دفعة
- عدد الطلبات المحتملة
- درجات المطابقة المتاحة

**المطابقة التلقائية:**
```sql
-- عند إضافة مخزون جديد
TRIGGER auto_match_inventory_trigger
  → smart_auto_match_orders(batch_id)
  → إنشاء صفقات للمطابقات العالية
```

### 3. مع قسم الصفقات

**الربط:**
- عرض درجة المطابقة لكل صفقة
- تتبع المصدر (تلقائي/يدوي)
- تحليل نجاح المطابقات

**البيانات المشتركة:**
```typescript
interface Deal {
  // ... باقي الحقول
  match_score?: number;
  match_source: 'ai_auto' | 'manual' | 'force_match';
  match_reasons?: MatchReason[];
}
```

### 4. مع قسم التحليلات

**المقاييس المشتركة:**
- معدلات نجاح المطابقة
- متوسط درجات المطابقة
- الأنماط المكتشفة
- أداء الموردين في المطابقة

**لوحة التحكم الرئيسية:**
```typescript
<MatchingAnalytics />
// تظهر في قسم Dashboard
```

### 5. مع نظام الإشعارات

**الإشعارات التلقائية:**
- مطابقة عالية الجودة وُجدت
- مطابقة أفضل متاحة
- مخزون جديد يطابق طلبك
- تحديث درجات المطابقة

**التكامل:**
```typescript
// في Header/TopNavigation
<NotificationCenter phone={session?.phone} />
// يعرض إشعارات المطابقة الذكية
```

### 6. مع قسم المستخدمين

**ملفات المستخدمين:**
- إحصائيات مطابقة لكل مستخدم
- تفضيلات المطابقة الشخصية
- سجل المطابقات

**التفضيلات:**
```sql
matching_preferences (
  user_phone,
  preference_type,
  preferences
)
```

---

## 📈 مقاييس الأداء الرئيسية (KPIs)

### 1. معدل المطابقة (Match Rate)

```
معدل المطابقة = (عدد المطابقات الناجحة / عدد الطلبات) × 100
```

**الهدف**: ≥ 80%

### 2. متوسط درجة المطابقة (Average Match Score)

```
متوسط الدرجة = مجموع درجات المطابقة / عدد المطابقات
```

**الهدف**: ≥ 85

### 3. معدل القبول (Acceptance Rate)

```
معدل القبول = (المطابقات المقبولة / إجمالي المطابقات المقترحة) × 100
```

**الهدف**: ≥ 70%

### 4. الوقت حتى المطابقة (Time to Match)

```
متوسط الوقت = مجموع أوقات المطابقة / عدد المطابقات
```

**الهدف**: ≤ 1 ساعة

### 5. معدل التدخل اليدوي (Manual Intervention Rate)

```
معدل التدخل = (المطابقات اليدوية / إجمالي المطابقات) × 100
```

**الهدف**: ≤ 10%

---

## 🎯 أفضل الممارسات

### 1. مراقبة دورية

✅ **افعل:**
- راجع الأداء يومياً
- حلل الأنماط أسبوعياً
- راجع القواعد شهرياً
- حدّث القائمة السوداء عند الحاجة

❌ **لا تفعل:**
- إهمال المقاييس المنخفضة
- ترك قواعد قديمة نشطة
- تجاهل الأنماط المكتشفة

### 2. إدارة القواعد

✅ **افعل:**
- ابدأ بقواعد بسيطة
- اختبر القواعد الجديدة
- وثّق الهدف من كل قاعدة
- استخدم أولويات واضحة

❌ **لا تفعل:**
- إضافة قواعد متضاربة
- استخدام قواعد معقدة جداً
- تفعيل كل القواعد مرة واحدة

### 3. التدخل اليدوي

✅ **افعل:**
- وثّق السبب بوضوح
- استخدمه للحالات الاستثنائية فقط
- راجع السجلات دورياً

❌ **لا تفعل:**
- الاعتماد الكامل على التدخل اليدوي
- إجراء تدخلات بدون توثيق
- تجاهل نمط التدخلات المتكررة

### 4. تحليل البيانات

✅ **افعل:**
- استخدم فترات تحليل مختلفة
- قارن الأداء شهرياً
- تتبع الاتجاهات طويلة المدى
- طبّق التوصيات الآلية بعد التحقق

❌ **لا تفعل:**
- الاعتماد على فترة قصيرة فقط
- تجاهل الموسمية
- اتخاذ قرارات بناءً على بيانات محدودة

---

## 🔐 الأمان والصلاحيات

### مستويات الوصول

#### 1. مدير النظام (System Admin)

```typescript
permissions: {
  matching: {
    can_view: true,
    can_edit: true,
    can_force_match: true,
    can_block_match: true,
    can_manage_rules: true,
    can_manage_blacklist: true,
  }
}
```

#### 2. مدير السوق (Market Manager)

```typescript
permissions: {
  matching: {
    can_view: true,
    can_edit: false,
    can_force_match: true,
    can_block_match: true,
    can_manage_rules: false,
    can_manage_blacklist: false,
  }
}
```

#### 3. موظف الدعم (Support Staff)

```typescript
permissions: {
  matching: {
    can_view: true,
    can_edit: false,
    can_force_match: false,
    can_block_match: false,
    can_manage_rules: false,
    can_manage_blacklist: false,
  }
}
```

### سجل التدقيق

جميع الإجراءات الحساسة مسجلة:

```sql
matching_overrides (
  admin_phone,
  override_type,
  reason,
  created_at
)
```

يشمل:
- المطابقات المفروضة
- المطابقات المحظورة
- تعديلات القواعد
- إضافة/حذف من القائمة السوداء

---

## 🚨 استكشاف الأخطاء

### مشكلة: معدل مطابقة منخفض

**الأسباب المحتملة:**
1. عتبة درجة عالية جداً
2. قواعد صارمة جداً
3. قائمة سوداء كبيرة
4. نقص في المخزون المتاح

**الحلول:**
```typescript
// 1. راجع القواعد
SELECT * FROM matching_rules WHERE is_active = true;

// 2. راجع القائمة السوداء
SELECT * FROM matching_blacklist WHERE is_active = true;

// 3. خفّض العتبة مؤقتاً
UPDATE matching_rules
SET actions = jsonb_set(actions, '{min_score}', '65')
WHERE rule_type = 'score_threshold';

// 4. راجع المخزون
SELECT COUNT(*) FROM inventory_batches
WHERE status = 'active' AND available_quantity > 0;
```

### مشكلة: مطابقات منخفضة الجودة

**الأسباب:**
1. أوزان غير صحيحة
2. بيانات غير دقيقة
3. قواعد متضاربة

**الحلول:**
```typescript
// مراجعة متوسط الدرجات
SELECT AVG(score) FROM matching_scores
WHERE created_at >= NOW() - INTERVAL '7 days';

// مراجعة توزيع الدرجات
SELECT
  CASE
    WHEN score >= 90 THEN '90-100'
    WHEN score >= 80 THEN '80-89'
    WHEN score >= 70 THEN '70-79'
    ELSE 'Below 70'
  END as range,
  COUNT(*) as count
FROM matching_scores
GROUP BY range;
```

### مشكلة: تأخر في المطابقة

**الأسباب:**
1. عدد كبير من القواعد
2. استعلامات بطيئة
3. حمل على قاعدة البيانات

**الحلول:**
```sql
-- تحسين الفهارس
REINDEX TABLE matching_scores;
REINDEX TABLE matching_rules;

-- تحليل الأداء
EXPLAIN ANALYZE
SELECT * FROM calculate_match_score('order-id', 'batch-id');

-- تعطيل قواعد غير ضرورية
UPDATE matching_rules SET is_active = false
WHERE priority < 0;
```

---

## 📊 تقارير وإحصائيات

### تقرير الأداء الشهري

```sql
SELECT
  DATE_TRUNC('month', metric_date) as month,
  SUM(total_matches_attempted) as total_attempts,
  SUM(successful_matches) as successful,
  AVG(avg_match_score) as avg_score,
  (SUM(successful_matches)::float / SUM(total_matches_attempted) * 100) as success_rate
FROM matching_performance
WHERE metric_date >= NOW() - INTERVAL '6 months'
GROUP BY month
ORDER BY month DESC;
```

### أفضل 10 أنماط نجاح

```sql
SELECT * FROM analyze_matching_patterns()
ORDER BY confidence_score DESC
LIMIT 10;
```

### إحصائيات التدخل اليدوي

```sql
SELECT
  DATE_TRUNC('week', created_at) as week,
  override_type,
  COUNT(*) as count
FROM matching_overrides
WHERE created_at >= NOW() - INTERVAL '3 months'
GROUP BY week, override_type
ORDER BY week DESC, count DESC;
```

---

## 🎓 الخلاصة

نظام إدارة المطابقة الذكية يوفر:

✅ **تحكم كامل** في سلوك المطابقة التلقائية
✅ **مراقبة فورية** للأداء والإحصائيات
✅ **أدوات تدخل** للحالات الخاصة
✅ **تحليل ذكي** للأنماط والاتجاهات
✅ **أمان محكم** مع تسجيل شامل
✅ **تكامل سلس** مع جميع أقسام المنصة

مع هذه الأدوات، يمكن للإدارة:
- تحسين جودة المطابقات باستمرار
- الاستجابة السريعة للمشاكل
- اتخاذ قرارات مدروسة بناءً على البيانات
- ضمان رضا العملاء وكفاءة العمليات

---

## 📞 الدعم الفني

للمساعدة أو الاستفسارات:
- راجع الوثائق في `AI_MATCHING_SYSTEM.md`
- افحص سجلات التدقيق
- استخدم أدوات التشخيص المدمجة
- راجع قواعد البيانات في `supabase/migrations/`

**الملفات المهمة:**
- `/src/components/admin/sections/MatchingSection.tsx`
- `/src/components/admin/matching/*`
- `/supabase/migrations/create_ai_matching_management_system.sql`
